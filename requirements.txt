# TubeInsight 필수 패키지 — 설치: pip3 install -r requirements.txt

yt-dlp>=2025.1.1        # 유튜브 메타데이터·자막·댓글 수집 (라이브러리로 직접 사용)
edge-tts>=6.1.0         # 무료 한국어 나레이션 TTS (인터넷 필요)
numpy                   # 오디오 처리
soundfile               # 오디오 파일 읽기/쓰기
av                      # 오디오 길이 측정·포맷 변환 (PyAV)
imageio-ffmpeg          # ffmpeg 바이너리 포함 — 영상 합성(③ 탭)에 사용, 별도 설치 불필요
google-genai>=2.0.0     # 구글 Gemini SDK (나노바나나 이미지 & Omni 영상 생성) — Python 3.10 이상 필요
python-dotenv>=1.0.0    # .env 환경변수 관리

# ── 선택: 유튜브 자동 업로드 (③ 탭) ──────────────────────────────
google-api-python-client
google-auth-oauthlib

# ── 선택: 내 목소리 보이스 클로닝 (용량 수 GB) ─────────────────────
# torch
# qwen-tts
